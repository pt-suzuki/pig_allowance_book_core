# PHP CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-php/ for more details
#
version: 2
jobs:
  build:
    machine:
      image: circleci/classic:edge
    steps:
    - checkout
    - add_ssh_keys:
        fingerprints:
        - "49:ab:88:4b:8c:be:50:e1:23:66:1c:9f:b6:31:e3:d5"
    - run: ssh ${USER_NAME}@${HOST_NAME} "cd /home/webmaster/pig_allowance_book_core && git pull"

workflows:
  version: 2
  build_and_deploy:
    jobs:
    - build:
        filters:
          branches:
            only: /^master/